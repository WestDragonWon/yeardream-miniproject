apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb
spec:
  serviceName: mongodb
  replicas: 2
  selector:
    matchLabels:
      app: mongodb
  template:
    metadata:
      labels:
        app: mongodb
    spec:
      containers:
        - name: mongodb
          image: mongo:4.4.18
          command:
            - "numactl"
            - "--interleave=all"
            - "mongod"
            - "--bind_ip"
            - "0.0.0.0"
            - "--replSet"
            - "MainRepSet"
            - "--setParameter"
            - "authenticationMechanisms=SCRAM-SHA-1"
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongodb-storage
              mountPath: /data/db  # MongoDB의 데이터 디렉토리
  volumeClaimTemplates:
    - metadata:
        name: mongodb-storage  # PVC 이름 명시
      spec:
        accessModes: [ "ReadWriteMany" ]
        storageClassName: efs-mongo  # 수정된 스토리지 클래스 이름
        resources:
          requests:
            storage: 1Gi
